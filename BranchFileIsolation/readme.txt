我经常为需要在不同git的不同branch种切换仅仅是为了浏览代码而头大
Sandboxie可以提供文件隔离的特性，对文件进行重定向。这可不可以用在branch上？这样就不用频繁切换branch了，实现同时浏览多个branch的效果

I'm always haunted by switchc between branchs in git just to browse the code
Sandboxie can provide file isolation and redirect files. Can this be used on git branch? So we don't have to switch branches frequently, we can work on different branch in one computer at the same time without switching. we can view at different branch at the same time
